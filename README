editor.codeactionsOnSave
trocar por editor.formatOnSave: true

estudar mais como usar o display em codeactionsOnSave


through example = through the humanities story
<strong> = negrito? <b>
        <i> italico

            id ids just for single elements

            class is for multiple elements